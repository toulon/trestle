!!!
html
  head
    link(rel='stylesheet', href='http://netdna.bootstrapcdn.com/bootstrap/3.0.3/css/bootstrap.min.css')
    script(src='https://ajax.googleapis.com/ajax/libs/jquery/1.7.2/jquery.min.js')
  body
  - if (typeof form !== 'undefined')
    div.row-fluid
      div.span6.offset3
        form(action='/',method='post').well.form-horizontal
          fieldset
            legend Comment about a product
            != form
          div.form-actions
            input(type='submit').btn.btn-large.btn-primary
  - else
    p form is undefined